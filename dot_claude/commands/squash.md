---
description: Break wip commits in git history into sane commit chunks
---

Several `wip` commits exist in git history.

Use the Agent tool with a general-purpose subagent to break these down into sane commit chunks.
